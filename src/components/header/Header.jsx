
const Header = (Props) => {
    return (
        <header>
            <nav>
                <a href="/">Главная</a>
                <a href="/">Каталог</a>
                <a href="/">Дом</a>
            </nav>  
        </header>
    );
};

export default Header